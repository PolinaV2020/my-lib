import React from "react";
import Navigation from "../Navigation/Navigation.js";

const Header = (props) => {
  return (
    <header>
      <Navigation />
    </header>
  );
};

export default Header;
